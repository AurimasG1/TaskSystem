apiVersion: v1
kind: Service
metadata:
  name: tasksystem-api
  namespace: tasksystem
  labels:
    app.kubernetes.io/name: tasksystem
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: tasksystem
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 8080
      targetPort: http
