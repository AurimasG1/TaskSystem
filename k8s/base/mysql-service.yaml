apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: tasksystem
  labels:
    app.kubernetes.io/name: mysql
    app.kubernetes.io/part-of: tasksystem
spec:
  selector:
    app.kubernetes.io/name: mysql
  ports:
    - name: mysql
      port: 3306
      targetPort: mysql
