dotnet new sln -n KuroOpti

dotnet new webapi -n KuroOpti.API
dotnet new classlib -n KuroOpti.Entities
dotnet new classlib -n KuroOpti.Data
dotnet new classlib -n KuroOpti.Repositories
dotnet new classlib -n KuroOpti.Services
dotnet new console -n KuroOpti.Console
dotnet new xunit -n KuroOpti.Services.Test

dotnet sln add KuroOpti.API
dotnet sln add KuroOpti.Entities
dotnet sln add KuroOpti.Data
dotnet sln add KuroOpti.Repositories
dotnet sln add KuroOpti.Services
dotnet sln add KuroOpti.Console
dotnet sln add KuroOpti.Services.Test

dotnet add KuroOpti.Data reference KuroOpti.Entities
dotnet add KuroOpti.Repositories reference KuroOpti.Data
dotnet add KuroOpti.Services reference KuroOpti.Repositories
dotnet add KuroOpti.API reference KuroOpti.Services



dotnet add KuroOpti.Data package Pomelo.EntityFrameworkCore.MySql
dotnet add KuroOpti.Data package Microsoft.EntityFrameworkCore.Design
dotnet add KuroOpti.API package Microsoft.EntityFrameworkCore.Tools


version: '3.8'

services:
  db:
    image: mysql:8
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: myapp
      MYSQL_USER: myapp
      MYSQL_PASSWORD: myapp123
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data:


docker compose up -d


CREATE USER 'kuroadmin'@'%' IDENTIFIED BY 'biggerpwd';
GRANT ALL PRIVILEGES ON KuroOpti.* TO 'kuroadmin'@'%';
FLUSH PRIVILEGES;


git init

.gitignore
bin/
obj/
.vs/
*.user
*.suo
*.db
appsettings.Development.json

git add .
git commit -m "Initial"
sukurti Repo Githube
git remode add origin https
git branch -M main
git push -u origin main


dotnet ef migrations add InitialCreate -p KuroOpti.Data -s KuroOpti.Console
dotnet ef database update -p KuroOpti.Data -s KuroOpti.Console
dotnet ef migrations script --idempotent -o deploy.sql -p KuroOpti.Data -s KuroOpti.Console

{
	"ConnectionStrings": {
		"DefaultConnection": "server=localhost;port=3306;database=KuroOpti;user=kuroadmin;password=biggerpwd"
	}
}

{
	"ConnectionStrings": {
		"DefaultConnection": "server=turntable.proxy.rlwy.net;port=55247;database=KuroOpti;user=teamuser;password=KuroOpti123!"
	}
}


docker run --name mysql8 \
  -e MYSQL_ROOT_PASSWORD=root \
  -e MYSQL_DATABASE=tasksystem \
  -p 3306:3306 \
  -d mysql:8


git clone https://github.com/AurimasG1/MovieCatalog.git